# CLI Reference

Command reference for sigillum. Run `sigillum <command> --help` for the
authoritative, always-current flag set.

_Command pages are generated by `gtb generate docs --command <name>`._

The command table below is regenerated on `gtb generate command` / `add-flag`.
Any prose you add outside the `gtb:commands` markers is preserved.

<!-- gtb:commands:start -->
<!-- gtb:commands:end -->
